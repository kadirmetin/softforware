import { createStyles } from "@mantine/core";

export const useNotificationStyles = createStyles({
  avatar: {
    cursor: "pointer",
  },
  bullet: {
    width: "10px",
    height: "10px",
    borderRadius: "50%",
    backgroundColor: "hsl(244, 78%, 58%)",
  },
});
